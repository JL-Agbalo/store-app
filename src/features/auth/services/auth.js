// Authentication service module
// Handles API calls for authentication operations like login, register, logout

// Import statements would go here...

// Function to handle user registration
// Expects: { email, password, name } credentials
// Returns: Promise with user data or error

// Function to handle user login
// Expects: { email, password } credentials
// Returns: Promise with auth token and user data

// Function to handle user logout
// Clears authentication state and local storage
